<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 当 RadioButton 被选中时的颜色 -->
    <item android:color="#0099ff" android:state_checked="true" />
    <!-- 默认的颜色 -->
    <item android:color="#cdcdcd" android:state_checked="false"/>
</selector>